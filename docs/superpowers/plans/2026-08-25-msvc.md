# Windows 工具链迁移 MSVC 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 Windows 工具链从 MSYS2 + MinGW-w64 g++ 切换为 MSVC(Ninja + cl,自动装 Build Tools),SwiftShader/abseil 池内 MSVC 编译,Qt6 走 aqtinstall 预编译,解决 `__nop` 编译错误。

**Architecture:** 保留 `-G Ninja` + 池架构;编译器从 MinGW 换成 MSVC cl。新增 `tools/deps_lib/msvc.sh`(vswhere 检测 / 自动装 Build Tools / 导出 vcvars),`win-deps.sh` 不再装 MinGW 编译器、改用 aqtinstall 拉 Qt6 MSVC 预编译;`pool.py` 的 `is_pacman_provided` 恒 False;`cmake_driver` 注入 MSVC 前缀 + `CMAKE_MSVC_RUNTIME_LIBRARY`;`setup-env.sh` 探测 MSVC。Linux 路径不改。

**Tech Stack:** Bash(MSYS2/MSVC 脚本)、Python(池驱动)、CMake + Ninja + MSVC cl、aqtinstall(Qt6 预编译)、SwiftShader/abseil(池内 MSVC)。

**Spec:** `docs/superpowers/specs/2026-08-25-msvc-design.md`

## Global Constraints

- 保留 `-G Ninja` 生成器,不切 `-G "Visual Studio"`。
- **不做 MinGW 回退**:工具/依赖缺失 → 补齐走主路径,或如实报告阻塞。
- `is_pacman_provided` 在 MSVC 下恒 `False`;deps.yaml 去掉 `windows_package` 字段。
- 平台差异只允许出现在:依赖部署脚本分支、窗口 surface、软件光栅驱动、环境变量。
- 现有测试必须保持全绿(当前 `tools/tests` 28 个 + 双项目测试)。
- golden 基线单一,双平台一致。
- MSVC 编译统一动态 CRT:`-DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreadedDLL`。

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `tools/deps_lib/msvc.sh` | **新增**:vswhere 检测 VS/Build Tools → 找不到自动装 → 导出 vcvars 路径 |
| `tools/win-deps.sh` | 改:①不装 MinGW 编译器;③去 windows_package;④Vulkan 改 MSVC 兼容;⑥Qt6 走 aqtinstall;⑦env.sh 写 MSVC 前缀 |
| `tools/deps_lib/pool.py` | 改:`is_pacman_provided` 恒 False;删 `_windows_package` |
| `tools/deps_lib/cmake_driver.py` | 改:`configure_command` 去 `/mingw64` 注入,加 MSVC 前缀 + `MSVC_RUNTIME_LIBRARY` |
| `tools/setup-env.sh` | 改:probe Windows 分支改探测 MSVC(ninja + vcvars + glslc/vulkan.h) |
| `third_party/deps.yaml` | 改:去掉 `windows_package` 字段 |
| `tools/tests/test_cmake_driver.py` | 加:MSVC 注入测试 |
| `tools/tests/test_pool.py` | 加:`is_pacman_provided` 恒 False 测试 |

---

### Task 1: 新增 `tools/deps_lib/msvc.sh`(检测 + 自动装 Build Tools + 导出 vcvars)

**Files:**
- Create: `tools/deps_lib/msvc.sh`
- Test: `tools/tests/test_msvc.sh`(bash 单测)

**Interfaces:**
- Consumes: 无(独立 shell 库)
- Produces:
  - `msvc_locate()` → 找到含 VC 工具链的 VS/Build Tools 安装根(如 `/c/Program Files/Microsoft Visual Studio/2022/BuildTools`),找不到返回 1
  - `msvc_vcvars_path(root)` → `<root>/VC/Auxiliary/Build/vcvars64.bat`(POSIX 路径)
  - `msvc_ensure()` → locate 成功直接返回;失败自动装 Build Tools;返回安装根或 die
  - 写 `.user-deps/vcvars.sh`:`export VS_INSTALL_ROOT=<root>` + vcvars64.bat 路径

- [ ] **Step 1: 写失败测试**

`tools/tests/test_msvc.sh`(bash 单测,验证纯函数逻辑;真实 vswhere 不在 CI 里跑):

```bash
#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
# 复用 msvc.sh 的函数定义但不执行自动安装
source "$ROOT/deps_lib/msvc.sh"

# 1) vcvars 路径拼接
got="$(msvc_vcvars_path "/c/Program Files/Microsoft Visual Studio/2022/BuildTools")"
[ "$got" = "/c/Program Files/Microsoft Visual Studio/2022/BuildTools/VC/Auxiliary/Build/vcvars64.bat" ] \
  || { echo "FAIL vcvars_path: $got"; exit 1; }
echo "PASS msvc_vcvars_path"

# 2) 安装根判定(空输入 → 无)
if msvc_locate >/dev/null 2>&1; then
  echo "INFO msvc_locate found (host has VS)"
else
  echo "INFO msvc_locate none (expected in CI)"
fi
echo "PASS msvc_locate no-op"
```

- [ ] **Step 2: 运行测试确认失败**

Run: `bash tools/tests/test_msvc.sh`
Expected: FAIL with `source: deps_lib/msvc.sh: No such file or directory`

- [ ] **Step 3: 写 `tools/deps_lib/msvc.sh`**

```bash
#!/usr/bin/env bash
# MSVC 工具链发现 + 自动安装(Build Tools)+ vcvars 导出。由 win-deps.sh source。
# 不单独执行。
set -euo pipefail

info() { printf '[INFO] %s\n' "$*"; }
err()  { printf '[ERROR] %s\n' "$*" >&2; }
die()  { err "$*"; exit 1; }
has()  { command -v "$1" >/dev/null 2>&1; }

# vswhere 默认路径(32 位安装的 x86 程序,安装到 64 位系统)
VSWHERE="/c/Program Files (x86)/Microsoft Visual Studio/Installer/vswhere.exe"
[ -x "$VSWHERE" ] || VSWHERE="$(command -v vswhere.exe 2>/dev/null || true)"

# msvc_vcvars_path <root> : 输出 vcvars64.bat 的 POSIX 路径。
msvc_vcvars_path() {
  printf '%s/VC/Auxiliary/Build/vcvars64.bat' "$1"
}

# msvc_locate : 输出含 VC 工具链 + Windows SDK 的 VS/Build Tools 安装根;找不到返回 1。
msvc_locate() {
  if [ -n "$VSINSTALLDIR" ] && [ -x "$(msvc_vcvars_path "$VSINSTALLDIR")" ]; then
    printf '%s' "$VSINSTALLDIR"; return 0
  fi
  if [ -n "$VSWHERE" ]; then
    local root
    root="$("$VSWHERE" -latest -products '*' -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -requires Microsoft.VisualStudio.Component.Windows10SDK -property installationPath 2>/dev/null | tr -d '\r' || true)"
    if [ -n "$root" ] && [ -x "$(msvc_vcvars_path "$root")" ]; then
      # vswhere 输出 Windows 路径,转 POSIX
      if has cygpath; then root="$(cygpath -u "$root" 2>/dev/null || printf '%s' "$root")"; fi
      printf '%s' "$root"; return 0
    fi
  fi
  return 1
}

# msvc_install_buildtools : 下载 vs_buildtools.exe 并静默装 Build Tools(免管理员,用户级)。
# 组件:VC 工具链 + Windows SDK + Ninja。
msvc_install_buildtools() {
  local dl="$1/buildtools-installer.exe"
  info "下载 VS Build Tools 安装器到 $dl ..."
  curl -fL --retry 3 -o "$dl" \
    "https://aka.ms/vs/17/release/vs_buildtools.exe" || die "下载 vs_buildtools.exe 失败(需能出网)"
  info "静默安装 Build Tools(约 2GB,含 VC + WinSDK + Ninja)..."
  "$dl" --quiet --norestart --nocache \
    --add Microsoft.VisualStudio.Workload.VCTools --includeRecommended \
    --wait --force || { rm -f "$dl"; die "Build Tools 安装失败(可手动重跑安装器)"; }
  rm -f "$dl"
}

# msvc_ensure : 定位 MSVC 安装根;无则自动装。返回安装根。
msvc_ensure() {
  local root
  if root="$(msvc_locate)"; then
    info "MSVC 已安装: $root"
    printf '%s' "$root"; return 0
  fi
  info "未检测到 MSVC(Build Tools)。将自动安装 ..."
  msvc_install_buildtools "$(dirname "${BASH_SOURCE[0]}")/../../.user-deps" || die "Build Tools 自动安装失败"
  # 安装后重新定位
  if root="$(msvc_locate)"; then
    info "MSVC 安装完成: $root"
    printf '%s' "$root"; return 0
  fi
  die "Build Tools 已安装但定位失败。请手动运行 vs_buildtools.exe 或设置 VSINSTALLDIR 指向其安装根。"
}

# msvc_write_vcvars_sh <root> : 写 .user-deps/vcvars.sh(记录 vcvars64.bat 路径)。
msvc_write_vcvars_sh() {
  local root="$1" out="${2:-}"
  [ -n "$out" ] || out="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)/.user-deps/vcvars.sh"
  mkdir -p "$(dirname "$out")"
  cat > "$out" <<EOF
# MSVC 环境(由 deps_lib/msvc.sh 生成)。构建前 source 以拿到 vcvars64 环境。
export VS_INSTALL_ROOT="$root"
export VC_VARS_BAT="$(msvc_vcvars_path "$root")"
# 用法:cmd /c "\"\$VC_VARS_BAT\" && set" 可导出完整环境;cmake 会自动找到 cl。
EOF
  info "已生成 $out"
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `bash tools/tests/test_msvc.sh`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add tools/deps_lib/msvc.sh tools/tests/test_msvc.sh
git commit -m "feat(tools): msvc.sh 检测/自动装 Build Tools,导出 vcvars"
```

---

### Task 2: `win-deps.sh` 切 MSVC(去 MinGW 工具链 + 去 windows_package + Qt6 aqtinstall + env.sh)

**Files:**
- Modify: `tools/win-deps.sh`
- Test: `tools/tests/test_win_deps_msvc.sh`(bash 单测,验证生成的 env.sh 内容)

**Interfaces:**
- Consumes: `tools/deps_lib/msvc.sh`(msvc_ensure / msvc_write_vcvars_sh)
- Produces:
  - `.user-deps/env.sh` 含 `MINE_ROOT` / `USER_DEPS` / `VK_ICD_FILENAMES` / `VK_DRIVER_FILES` / `CMAKE_PREFIX_PATH`(MSVC 池前缀 + Qt6 前缀),**不含 `/mingw64`**
  - 不再装 MinGW 编译器

- [ ] **Step 1: 写失败测试**

`tools/tests/test_win_deps_msvc.sh`(mock 掉 pacman/aqtinstall,只验证 env.sh 生成逻辑):

```bash
#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
USER_DEPS="$ROOT/.user-deps"
rm -rf "$USER_DEPS"
mkdir -p "$USER_DEPS"

# 复用 win-deps.sh 的 env.sh 生成段(抽出为函数前,直接测关键不变量)
# 1) env.sh 不含 /mingw64
cat > "$USER_DEPS/env.sh" <<EOF
export MINE_ROOT="$ROOT"
export CMAKE_PREFIX_PATH="/mingw64:/c/qt"
EOF
if grep -q "mingw64" "$USER_DEPS/env.sh"; then
  echo "FAIL: env.sh 仍含 /mingw64"; exit 1
fi
echo "PASS env.sh no /mingw64"
rm -rf "$USER_DEPS"
```

- [ ] **Step 2: 运行测试确认失败**

Run: `bash tools/tests/test_win_deps_msvc.sh`
Expected: FAIL with "env.sh 仍含 /mingw64"

- [ ] **Step 3: 改 `tools/win-deps.sh`**

关键改动(保留 ①镜像源 / ②基础工具链的 ninja 检测;去掉 MinGW 编译器):

```bash
# 顶部:source msvc.sh
# shellcheck disable=SC1091
. "$MINE_ROOT/tools/deps_lib/msvc.sh"

# ②基础工具链:不再装 MinGW g++,只保证 ninja/git/python3/glslc
MISS_TOOLS=()
has ninja    || MISS_TOOLS+=(mingw-w64-x86_64-ninja)
has git      || MISS_TOOLS+=(git)
has python3  || MISS_TOOLS+=(mingw-w64-x86_64-python)
has python3 && ! python3 -c 'import yaml' >/dev/null 2>&1 && MISS_TOOLS+=(mingw-w64-x86_64-python-yaml)
if [ "${#MISS_TOOLS[@]}" -gt 0 ]; then
  info "② pacman 安装基础工具链: ${MISS_TOOLS[*]}"
  pacman -S --needed --noconfirm "${MISS_TOOLS[@]}"
else
  info "② 基础工具链齐全"
fi

# ③ windows_package:MSVC 下不再用 pacman 预编译 → 删除该块(pool.is_pacman_provided 恒 False)

# ④ Vulkan:glslc/vulkan.h 改用 MSVC 兼容来源(MSYS2 的 shaderc 头 glslc 可用,但 .h 用 Vulkan SDK 或 MSYS2)
#   (保留现有 mingw-w64-x86_64-vulkan-headers/shaderc/loader 即可,glslc.exe 与 vulkan.h 兼容 MSVC)

# ⑥ Qt6:改走 aqtinstall(MSVC 预编译)
install_qt_msvc() {
  local qt_root="$USER_DEPS/qt"
  if [ -f "$qt_root/msvc2019_64/bin/qmake.exe" ]; then
    info "⑥ Qt6 MSVC 已存在,跳过"; return 0
  fi
  has python3 || die "python3 缺失"
  python3 -m pip install --quiet aqtinstall || die "aqtinstall 安装失败"
  mkdir -p "$qt_root"
  info "⑥ aqtinstall 下载 Qt6 MSVC 预编译(约 1GB)..."
  python3 -m aqt install-qt windows desktop 6.5.3 win64_msvc2019_64 \
    --outputdir "$qt_root" --modules qtbase || die "Qt6 下载失败"
}

# ⑦ env.sh:MSVC 前缀 + Qt6 前缀 + SwiftShader ICD
cat > "$USER_DEPS/env.sh" <<EOF
export MINE_ROOT="$MINE_ROOT"
export USER_DEPS="$USER_DEPS"
export VK_ICD_FILENAMES="$SWSS_ICD"
export VK_DRIVER_FILES="$SWSS_ICD"
export CMAKE_PREFIX_PATH="$USER_DEPS/qt/msvc2019_64:$MINE_ROOT/third_party/_install/glfw-3.4/release"
EOF
```

- [ ] **Step 4: 运行测试确认通过**

Run: `bash tools/tests/test_win_deps_msvc.sh`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add tools/win-deps.sh tools/tests/test_win_deps_msvc.sh
git commit -m "feat(tools): win-deps 切 MSVC(去 MinGW 编译器/去 windows_package/Qt6 aqtinstall/env.sh MSVC 前缀)"
```

---

### Task 3: `pool.py` 的 `is_pacman_provided` 恒 False + 删 `_windows_package`

**Files:**
- Modify: `tools/deps_lib/pool.py:21-44`
- Test: `tools/tests/test_pool.py`

**Interfaces:**
- Consumes: 无
- Produces: `is_pacman_provided(root, name)` 恒 `False`(MSVC 下 pacman 的 MinGW 库不兼容)

- [ ] **Step 1: 写失败测试**

`tools/tests/test_pool.py` 追加:

```python
    def test_is_pacman_provided_false_on_windows_msvc(self):
        # MSVC 工具链下 pacman 的 MinGW 库不兼容 → 恒 False
        self.assertFalse(pool.is_pacman_provided(self.root, "abseil-cpp"))
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python3 -m unittest test_pool -v`
Expected: FAIL(旧逻辑在 Windows 下返回 pacman 状态;Linux 下恒 False,故需在测试里 mock on_windows 或接受当前行为)

- [ ] **Step 3: 改 `pool.py`**

```python
def is_pacman_provided(root: str, name: str) -> bool:
    """MSVC 工具链下 pacman 的 MinGW 预编译库不兼容 → 恒 False(不再用 pacman 预编译)。"""
    return False
```

删除 `_windows_package`(不再被引用)。

- [ ] **Step 4: 运行测试确认通过**

Run: `python3 -m unittest test_pool -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add tools/deps_lib/pool.py tools/tests/test_pool.py
git commit -m "fix(tools): is_pacman_provided 恒 False(MSVC 下不用 pacman 预编译)"
```

---

### Task 4: `cmake_driver.configure_command` 去 `/mingw64` 注入,加 MSVC 前缀 + `MSVC_RUNTIME_LIBRARY`

**Files:**
- Modify: `tools/deps_lib/cmake_driver.py:25-44`
- Test: `tools/tests/test_cmake_driver.py`

**Interfaces:**
- Consumes: `pool.is_pacman_provided`(Task 3,恒 False)、`_built_prefixes`(现有)
- Produces: `configure_command(root, lib, variant)` 在 Windows 下:
  - 不注入 `/mingw64`
  - 注入已 build 池前缀 + `-DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreadedDLL`

- [ ] **Step 1: 写失败测试**

`tools/tests/test_cmake_driver.py` 追加:

```python
class TestMsvcConfigureCommand(unittest.TestCase):
    def _make_pool(self):
        tmp = tempfile.TemporaryDirectory(); self.addCleanup(tmp.cleanup)
        root = tmp.name
        d = os.path.join(root, "third_party", "_install", "abseil-cpp-20260817.0", "release")
        os.makedirs(d); open(os.path.join(d, ".built"), "w").write("")
        return root

    def test_no_mingw64_and_msvc_runtime(self):
        root = self._make_pool()
        lib = LibSpec(name="fmt", repo="r", tag="10.2.1")
        cmd = cmake_driver.configure_command(root, lib, "release")
        joined = " ".join(cmd)
        self.assertNotIn("/mingw64", joined)
        self.assertIn("-DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreadedDLL", joined)
        # 注入已 build 的 MSVC 池前缀(abseil)
        self.assertIn(os.path.join(root, "third_party", "_install", "abseil-cpp-20260817.0", "release"), joined)
```

(注:需在 Windows 语义下测试——`configure_command` 现有逻辑用 `pool.on_windows()` 决定 `/mingw64` 注入;该测试在 Linux 下 `on_windows()` 为 False 已天然不注入,但 MSVC_RUNTIME_LIBRARY 与池前缀应恒加,不受 on_windows 影响。)

- [ ] **Step 2: 运行测试确认失败**

Run: `python3 -m unittest test_cmake_driver -v`
Expected: FAIL(`MSVC_RUNTIME_LIBRARY` 未注入)

- [ ] **Step 3: 改 `cmake_driver.py`**

```python
def configure_command(root: str, lib: LibSpec, variant: str) -> list:
    src = pool.src_dir(root, lib.name, lib.tag)
    bdir = pool.build_dir(root, lib.name, lib.tag, variant)
    idir = pool.install_dir(root, lib.name, lib.tag, variant)
    cmd = [
        "cmake", "-S", src, "-B", bdir, "-G", "Ninja",
        "-DCMAKE_BUILD_TYPE=" + variant,
        "-DCMAKE_INSTALL_PREFIX=" + idir,
    ]
    for opt in lib.options:
        cmd.append("-D" + opt)
    # 注入池内已建前缀,使 find_package(absl) 等能命中池产物
    prefixes = _built_prefixes(root, variant)
    if prefixes:
        cmd.append("-DCMAKE_PREFIX_PATH=" + ";".join(prefixes))
    # MSVC 工具链:统一动态 CRT,避免静态/动态 ABI 冲突
    if pool.on_windows():
        cmd.append("-DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreadedDLL")
    return cmd
```

(删除 `/mingw64` 注入分支。)

- [ ] **Step 4: 运行测试确认通过**

Run: `python3 -m unittest test_cmake_driver -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add tools/deps_lib/cmake_driver.py tools/tests/test_cmake_driver.py
git commit -m "fix(tools): cmake_driver 去 /mingw64 注入,加 MSVC_RUNTIME_LIBRARY"
```

---

### Task 5: `setup-env.sh` probe Windows 分支改探测 MSVC

**Files:**
- Modify: `tools/setup-env.sh:53-69, 71-85, 80-84`
- Test: `tools/tests/test_setup_env_probe.sh`(bash 单测)

**Interfaces:**
- Consumes: `tools/deps_lib/msvc.sh`(msvc_locate / msvc_ensure)
- Produces: `probe()` 在 Windows 下探测 MSVC(cl 可经 vcvars + ninja + glslc/vulkan.h),不再要求 g++/pkg-config

- [ ] **Step 1: 写失败测试**

`tools/tests/test_setup_env_probe.sh`:

```bash
#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
# 验证 setup-env.sh 中 probe 的 Windows 分支不探测 g++(改为 MSVC)
grep -q 'chk "g++"' "$ROOT/setup-env.sh" && {
  echo "FAIL: probe 仍探测 g++(应改 MSVC)"
  exit 1
}
echo "PASS probe no g++ hard-dep"
grep -q "msvc" "$ROOT/setup-env.sh" && echo "PASS probe references msvc" || {
  echo "FAIL: probe 未引用 msvc"
  exit 1
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `bash tools/tests/test_setup_env_probe.sh`
Expected: FAIL(g++ 仍被探测)

- [ ] **Step 3: 改 `setup-env.sh`**

在 `probe()` 的 Windows 分支:

```bash
if [ "$OS_PLATFORM" = "windows" ]; then
  # Windows:探测 MSVC(vcvars + cl)+ ninja + glslc/vulkan.h;不再硬要求 g++/pkg-config
  chk "ninja"  "" "ninja --version"
  chk "python3" "3.8" "python3 --version"
  # MSVC 定位(经 msvc.sh)
  # shellcheck disable=SC1091
  . "$MINE_ROOT/tools/deps_lib/msvc.sh"
  if msvc_locate >/dev/null 2>&1; then
    info "[OK]   MSVC: $(msvc_locate)"
  else
    HARD_MISS=$((HARD_MISS+1)); MISS_DETAILS+=("MSVC(需 VS Build Tools)")
    printf '[MISS] MSVC: 未安装\n'
  fi
  chk_user_deps_windows
else
  chk "cmake" "3.22" "cmake --version"
  chk "g++"   "11"   "g++ --version"
  chk "pkg-config" "" "pkg-config --version"
  chk_user_deps
fi
chk "git" "" "git --version"
```

(同时把 Linux 分支的 `chk "cmake"` 移到 Linux 内;`git`/`python3` 保持公共。)

- [ ] **Step 4: 运行测试确认通过**

Run: `bash tools/tests/test_setup_env_probe.sh`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add tools/setup-env.sh tools/tests/test_setup_env_probe.sh
git commit -m "feat(tools): setup-env probe Windows 改探测 MSVC"
```

---

### Task 6: deps.yaml 去掉 `windows_package` 字段

**Files:**
- Modify: `third_party/deps.yaml`

**Interfaces:**
- Consumes: 无
- Produces: deps.yaml 无 `windows_package`;`manifest.extract_windows_packages` 返回空(其单测保留,验证空场景)

- [ ] **Step 1: 改 deps.yaml**

删除 abseil-cpp 条目下的 `windows_package: mingw-w64-x86_64-abseil-cpp` 及其注释。

- [ ] **Step 2: 验证**

```bash
python3 -c "import sys; sys.path.insert(0,'tools'); from deps_lib import manifest; m=manifest.load_global_manifest('.'); print(manifest.extract_windows_packages(m))"
# 预期输出:[](空)
```

- [ ] **Step 3: Commit**

```bash
git add third_party/deps.yaml
git commit -m "deps: 去掉 windows_package(MSVC 下不用 pacman 预编译)"
```

---

### Task 7: 全量回归 + 语法检查

**Files:**
- Test: `tools/tests/`(全部)

- [ ] **Step 1: 全量单测**

```bash
cd tools/tests && python3 -W error::ResourceWarning -m unittest discover -v
# 预期:全部 PASS(28 + 新增)
```

- [ ] **Step 2: bash 语法检查**

```bash
bash -n tools/win-deps.sh tools/setup-env.sh tools/deps_lib/msvc.sh && echo OK
bash tools/tests/test_msvc.sh
bash tools/tests/test_win_deps_msvc.sh
bash tools/tests/test_setup_env_probe.sh
```

- [ ] **Step 3: Commit(如有改动)**

```bash
git add -A && git commit -m "test: MSVC 迁移回归全绿" || true
```

---

## Self-Review

**Spec 覆盖:**
- §3.1 msvc.sh → Task 1 ✅
- §3.2 env.sh MSVC 前缀 + §3.4 win-deps(去 MinGW/Qt6 aqtinstall)→ Task 2 ✅
- §3.3 pool 恒 False → Task 3 ✅
- §3.3 cmake_driver 注入 → Task 4 ✅
- §3.5 probe MSVC → Task 5 ✅
- §3.4 deps.yaml 去 windows_package → Task 6 ✅
- §6 测试 → Task 7 ✅

**占位符扫描:** 无 TBD/TODO;每步含实际代码。Task 2 的 aqtinstall 版本号 `6.5.3` 为固定值,如未来升级需同步。

**类型一致性:** `msvc_locate`/`msvc_vcvars_path`/`msvc_ensure`/`msvc_write_vcvars_sh` 在 Task 1 定义、Task 2 消费,签名一致;`is_pacman_provided` 在 Task 3 定义、Task 4 消费,签名一致。
